Will use jupyter notebook

Environment:
pip install python-mnist~
wget --recursive --level=1 --cut-dirs=3 --no-host-directories --directory-prefix=data --accept '*.gz' http://yann.lecun.com/exdb/mnist/
pushd data
gunzip*
popd